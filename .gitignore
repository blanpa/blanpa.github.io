# Hugo build output. Any --destination used for a local check belongs here
# too, otherwise a stray `git add -A` commits a whole rendered site.
public/
_site/
.check/
resources/
.hugo_build.lock
vendor/
node_modules/
*.log
*.bak
